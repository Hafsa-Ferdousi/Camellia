// NOTE: The wireframe does NOT show a separate "Shop by Category" section with image cards.
// Category filtering is done via chip rows directly on the homepage (in Home.jsx).
// This component is kept as a no-op to avoid import errors — the chip filtering
// is handled directly in Home.jsx using the .chip / .chip-row CSS classes.
export default function CategorySection() {
  return null;
}
